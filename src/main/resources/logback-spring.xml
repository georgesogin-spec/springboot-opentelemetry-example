<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for local development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OpenTelemetry MDC instrumentation for log correlation -->
    <!-- Logs will be exported via the OpenTelemetry SDK configured in the application -->

    <!-- Root logger configuration -->
    <root level="INFO">
        <!-- Log to console for local visibility -->
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Application-specific logger with DEBUG level -->
    <logger name="com.example.demo" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Spring Framework loggers -->
    <logger name="org.springframework" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- OpenTelemetry SDK logger -->
    <logger name="io.opentelemetry" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>
</configuration>
